
const number = [1,4,9];

const roots = number.map(item => Math.sqrt(item));

console.log(roots);

//map method를 호출한 배열과 map 메서드가 생성하여 반환한 배열은 1:1 매핑

[1,2,3].map((item, index, arr)=>{
    console.log()
});